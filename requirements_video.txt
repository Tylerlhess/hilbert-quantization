# Additional requirements for video storage and search functionality
# These extend the base requirements.txt with video processing capabilities

# Core requirements (from requirements.txt)
numpy>=1.21.0
Pillow>=8.3.0

# Video processing requirements
opencv-python>=4.5.0
scikit-image>=0.19.0    # For advanced similarity metrics (SSIM)

# Optional but recommended for better performance
matplotlib>=3.5.0       # For visualization in demos
tqdm>=4.62.0           # For progress bars in batch processing
psutil>=5.8.0          # For memory monitoring

# Development and testing (optional)
pytest>=6.2.0
pytest-cov>=3.0.0
